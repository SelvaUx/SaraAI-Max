# SaraAI Max — Dependencies

# Core (required)
requests>=2.28.0
rich>=13.0.0

# System info (optional but recommended)
psutil>=5.9.0

# Voice I/O (optional — install with: pip install sara-ai[voice])
pyttsx3>=2.90
SpeechRecognition>=3.10.0
# pyaudio>=0.2.13  # May need: pip install pipwin && pipwin install pyaudio  (Windows)

# Screenshot support (optional)
pyautogui>=0.9.54
Pillow>=9.0.0
